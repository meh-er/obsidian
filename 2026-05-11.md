
```
#1
SELECT * 
FROM students;

#2
SELECT * 
FROM students
WHERE fees > 500;

#3
INSERT INTO students VALUES (5, Alice Brown, 700, Medicine);

#4
UPDATE fees=900 FROM students WHERE student_name = Matt Long;

#5
DELETE * 
FROM students
WHERE student_id = 3;

#6
SELECT COUNT(owner_id=2)
FROM todo_item;

#7
SELECT owner_id, COUNT(todo)
FROM todo_item
GROUP BY owner_id;

#8
SELECT owner_id, COUNT(todo)
FROM todo_item
GROUP BY owner_id
HAVING COUNT(todo) > 5;

#9
SELECT *
FROM students
ORDER BY fees DESC;

#10
SELECT s.student_name, e.class_id
FROM students s
JOIN enrollment e ON s.student_id = e.student_id;
```


JDL
```
#11
Entity Student {
	studentName String,
	fees Integer,
	dateOfBirth LocalDate
}

#12
Entity Researcher {
	nin UUID,
	birthDate LocalDate,
	salary BigDecimal,
	active Boolean
}

#13
relationship OneToMany {
	Department{employee} to Employee
}

#14
relationship ManyToMany {
	Students to Classes
}

#15
Entity Program {
	programName String,
	fees Integer
}

Entity Student {
	studentName String
}

relationship ManyToOne {
	Student to Program{student(studentName)}
}
```
sdfs

```
UPDATE students
SET fees = 900
WHERE student_name = 'Matt Long';

DELETE FROM students
WHERE student_id = 3;

SELECT COUNT(*)
FROM todo_item
WHERE owner_id = 2;

SELECT owner_id, COUNT(*)
FROM todo_item
GROUP BY owner_id;
ca
```

take 2
```
#1 
SELECT s.student_name, p.program_name, p.fees
FROM students s
JOIN programs p ON s.program_id = p.program_id
WHERE program_name = 'Medicine';

#2
SELECT d.department_name, e.employee_name
FROM departments d
LEFT JOIN employees e
ON d.department_id = e.department_id;

#3
SELECT s.student_name, c.class_name
FROM students s
JOIN enrollment e ON s.student_id = e.student_id
JOIN classes c ON e.class_id = c.class_id;

#4
CREATE TABLE programs (
	program_id Integer PRIMARY KEY,
	program_name VARCHAR(100),
	fees Integer
	);
	
#5
CREATE TABLE students (
	student_id Integer PRIMARY KEY,
	student_name VARCHAR(100),
	program_id Integer,
	FOREGIN KEY program_id REFERENCES programs(program_id)
	);

#6
CREATE TABLE enrollment (
	student_id Integer,
	class_id Integer
	COMPOSITE PRIMARY KEY class_id, student_id REFERENCES students(student_id), classes(class_id)
	);
	
#7
SELECT owner_id, AVG(days_taken)
FROM tasks
GROUP BY owner_id;

#8
SELECT owner_id, AVG(days_taken)
FROM tasks
GROUP BY owner_id
HAVING AVG(days_taken) > 7;

#9
SELECT customer_id, SUM(total_price)
FROM orders
GROUP BY customer_id
ORDER BY total_price DESC;

#10
entity Book {
	title String,
	pages Integer,
	publishedDate LocalDate
}

dto with mapstruct;
service with serviceClass;
pagination with pagination;

#11
entity UserProfile {
	username String required minlength(3) maxlength(20),
	age Integer min(18),
	bio TextBlob
}

#12
enum Priority {
	LOW, MEDIUM, HIGH
}

entity Task {
	title String,
	priority Priority
}

#13
entity Student {
	studentName String required
}

entity Course {
	courseName String required
}

entity Lecturer {
	lecturerName String required
}

relationship ManyToMany {
	Student to Course
}

relationship OneToMany {
	Course(lecturer) to Lecturer
}

#14
i know i got this wrong

#15 
i know i got this wrong

```

