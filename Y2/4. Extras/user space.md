#Theory 
The memory area where **application software** and some drivers execute, typically with one address space per process. Refers to all code that runs outside OS's kernel. Runs in its own user space.
e.g. C standard library: lib, init, malloc, pthread_create etc
