#Theory 

- A secure cookie is a HTTP cookie that has the secure attribute set.
- Means the user agent will include the cookie only in a HTTP request **only if** the request is transmitted over a secure channel. (e.g. HTTPS)
- Only protects cookie's confidentiality
- Network attackers can overwrite secure cookies and disrupt their integrity.