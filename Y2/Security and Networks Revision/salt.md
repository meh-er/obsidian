#SaN 

Password salting is an additional input to a **one-way function.** It helps defend against attacks that use **precomputed tables** (rainbow tables) and protects passwords that occur multiple times in a database, as knowing the salt does not help the attacker.