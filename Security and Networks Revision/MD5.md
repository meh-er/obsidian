#SaN 

- **Collision Vulnerabilities**: It is susceptible to collision attacks (where two different inputs produce the same hash)  
    
- **Reverse Engineering**: Attackers can reverse engineer (logic not the hash itself) the hash to find an input that matches (can make use of rainbow tables)  
    
- **Brute Force**: The speed at which hashes can be generated means its susceptible to brute force.