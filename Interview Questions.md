- What is [[ASLR]] and why is it important?
- What is the difference between a [[process]] and a [[thread]]?
- What determines whether memory is allocated on the [[stack]] vs the [[heap]]?
- What is a [[stack frame]]?
- What happens during a function call at the assembly level?
- What is the role of the [[instruction pointer]] and the [[base pointer]]

- What is a stack buffer overflow?
- What is a heap overflow?
- What is a use-after-free vulnerability?
- What is memory fragmentation?
- What is a stack canary and how can it detect an overflow?
- What happens if a canary value is changed?
- What is PIE (Position Independent Executable)?

