# 1
## a)
n = 35
q= 5
p = 7
e = 5
c = 9
Formula : c = m^e mod n

m = c^d mod n
de = 1 mod tot(n)
tot(n) = 4 * 6 = 24

5d = 1 mod 24
5 x 5 = 1 mod 24
 =9^5 mod 35
 =59049 mod 35 = 4
 **=m = 4**

**10/10**

## b)
Yes - it can't be found without knowing the counter that it has been xored with and what the counter has been incremented to be. By taking the last block it won't be a too long addition to a transmission.

**5/10**

# 2
## a
If the attacker knows the region of time that the order was generated they may be able to brute force it - insecure.  **10/10**

## b
Yes - the attacker will be able to find some of the alternate counters and then be able to reverse the process. the counter is prcessed w the round key and then xored to the plaintext to create ciphertext - so if there are limited values for the counter and the attacker can create orders (plaintext), should be able to guess the next counter values by looking at the logs?????

**8/10**
# 3
## a
root certificate is authorised by a certificate authority (CA). tls encrypts the communication but does not verify the server at the other end - it just sends teh certificate. If the certificate is malicious, then the installer will probably be able to read TLS-encrypted sessions because the client will still verify them, but with a corrupt certificate that gives the malware access  alongside the intended server.

**9/10**

## b
TLS protects attacks in the transit of client-server, but not necessarily the client or server themselves. cross site scripting attacks attack the user by modifying what they see/ taking their information from the browser. They are because of weaknesses with validation of user input and not using httponly tag and not having a good content security policy. they are not an issue of actual data transmission but rather the security of the website itself on the server. So no, XSS attacks are not prevented and users should take other precautions to be warey of them instead.

**10/10**