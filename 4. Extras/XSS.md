#Theory 

XSS enables attackers to inject client-side scripts into web pages viewed by other users.
Attackers use known vulnerabilities in web-based applications, servers or plug-in systems on which they rely. They fold malicious content into the content being delivered from the side.
There are persistent and non persistent vulnerabilities.

Persistent - saved by server
Non persistent - e.g. HTTP query parameters