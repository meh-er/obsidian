#Theory 

Network Address Translation is a service that operates on a router or edge platform to connect private networks to public networks like the internet.

An organisation needs one IP address to represent an entire group of devices as they connect outside their network.

- Can help reduce security risk by hiding internal networks from public networks.
- Separates public and private networks.