#Theory 

A **Demilitarized Zone** in network design is a physical or logical subnetwork that contains an organisation's external facing services to larger networks such as the internet.
It adds an additional layer of security to the LAN as an external network node can only access what is on the DMZ, and everything else is protected behind a [[firewall]].