#SaN 

A programme used to debug other programmes. Usually you can run or halt the target programme at any point, step through code line by line, and display or modify the contents of memory, CPU registers, and stack frames.