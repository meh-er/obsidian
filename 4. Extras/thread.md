#Theory 
- Often called 'lightweight processes' because they share some resources
- Each thread is always part of one specific process
- Has states Running, Ready, and Dispatched
- Takes less time to terminate, do not isolate
- e.g. mobile banking app, threads would include balance, touch inputs, image loading.