LIST COMPREHENSIONS
Use when:
- transforming each element in a list
- filtering
- combining two lists
Use map or filter when:
- transformation is 1 function call
- chaining operation

example of where
encodeModified :: String -> [Modd]
encodeModified x = map toModd (encode x)
	where
		toModd (1,y) = Single y
		toModd (n, y) = Multiple n y

nother example of where
where
	(l1, l2) = splitAt n list
	newL = if (length l1) == n then init l1 else l1

PROBLEM QUESTIONS
- [ ] q10
- [ ] q15
- [ ] randomness (q21 - 25)
